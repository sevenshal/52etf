[pytest]
testpaths = tests
# test_duckdb_utils.py 引用不存在的 analytics_database.connect_duckdb，
# 是长期 pre-existing 失败，与 AIStock 无关；忽略以便 pytest 全量可作为 canonical 命令。
addopts = --ignore=tests/test_duckdb_utils.py
